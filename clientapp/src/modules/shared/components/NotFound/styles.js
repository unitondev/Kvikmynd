import gif from '../../../../../public/404robot.gif'

const styles = {
  gifBlock: {
    backgroundImage: `url(${gif})`,
    backgroundPosition: 'center center',
    position: 'fixed',
    top: '0',
    left: '0',
    height: '100%',
    width: '100%',
  }
}

export default styles
